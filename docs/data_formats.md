## Image metadata
Image metadata is saved in `****.pkl` files, which can be read as:

```
with open(`****.pkl`,'rb') as file:
    img_metadata = pickle.load(file)
```

`img_metadata` then contains the following data:

```

```


## Lidar data


## Poses
